#include <stdio.h>
#include <stdlib.h>

int main()
{
    char buffer[255];
    char freq[255];

    printf("");
    scanf("%s", buffer);
    for (int i = ; i < (sizeof(buffer)); i++)
        printf(".");
    return 0;
}
